# Версии закреплены точно: бот сам себя обновляет, и «поехавшая» зависимость
# сломала бы его в момент, когда чинить его можно только через SSH.
#
# Это «лёгкий» базовый набор — его хватает для работы с ОБЛАЧНЫМ распознаванием
# голоса (Groq). Локальный faster-whisper тяжёлый (~400 МБ) и вынесен отдельно, в
# requirements-local-stt.txt: ставь его, только если хочешь распознавать без сети.

# Telegram и асинхронный HTTP
aiogram==3.31.0
aiohttp==3.14.3
aiofiles==25.1.0
python-dotenv==1.2.3

# Настройки и DTO
pydantic==2.13.5
pydantic-settings==2.15.0

# Внедрение зависимостей
dishka==1.10.1

# Postgres. Расширение pgvector ставится пакетом ОС postgresql-16-pgvector.
asyncpg==0.31.0

# Тесты: их гоняет preflight перед каждой выкаткой
pytest==9.1.1
pytest-asyncio==1.4.0
